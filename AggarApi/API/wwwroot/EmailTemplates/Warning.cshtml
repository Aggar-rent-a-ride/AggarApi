<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Warned</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f1f5f9;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }

        .header {
            background-color: #ec942c;
            padding: 16px 24px;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .icon-container {
            background-color: rgba(255, 255, 255, 0.2);
            padding: 8px;
            border-radius: 50%;
        }

        .header-text {
            color: white;
            font-size: 20px;
            font-weight: 600;
            margin: 0;
        }

        .content {
            padding: 24px;
        }

        .username {
            font-weight: 500;
            color: #1e293b;
        }

        .ban-details {
            background-color: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 16px;
            margin: 16px 0;
        }

        .ban-date {
            font-weight: 500;
            color: #1e293b;
        }

        .reason-label {
            font-weight: 500;
            color: #334155;
            margin-bottom: 4px;
        }

        .reason-text {
            color: #475569;
            margin: 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="header-text">Account Warned</h1>
        </div>

        <div class="content">
            <p style="color: #475569; margin-bottom: 16px;">
				Hello <span class="username">@Model.Name</span>, your account has been warned due to a violation of our community guidelines. There are @Model.WarningsLeft warnings left before your account is suspended.
            </p>
        </div>
    </div>
</body>
</html>